# AUTO-GENERATED by parser/translate_oracle.py -- do not hand-edit.
# Regenerate: python -m parser.parse_scryfall && python -m parser.translate_oracle

##############################
Adventurer's Inn

	Abilities:
		T: self.controller.mana.add(mana.Mana.COLORLESS, 1)

	Triggers:
		onEtB
			self.controller.gain_life(2)


##############################
Aerial Modification

	Aura:
		Targets:
			'creature'

		self.add_pt((2, 2))


##############################
Aether Adept

	Triggers:
		onEtB
			[t.bounce() for t in self.legal_targets]
				Targets:
					'target creature'


##############################
Aether Meltdown

	Aura:
		Targets:
			'creature'

		self.add_pt((-4, 0))


##############################
Airdrop Aeronauts

	Triggers:
		onEtB
			self.card.controller.gain_life(5)
				If:
					bool(self.card.controller.turn_events['permanent_left'])


##############################
Angelic Edict

	Targets:
		'target creature or enchantment'

	[t.exile() for t in self.legal_targets]


##############################
Annihilating Fire

	Targets:
		'any target'

	[t.take_damage(self, 3) for t in self.legal_targets]


##############################
Annul

	Targets:
		'artifact or enchantment spell'

	[t.counter(source=self) for t in self.legal_targets]


##############################
Aquus Steed

	Abilities:
		2U, T: [t.add_effect('modifyPT', (-2, 0), self, self.game.eot_time) for t in self.legal_targets]
			Targets:
				'target creature'


##############################
Artisan's Sorrow

	Targets:
		'target artifact or enchantment'

	[t.destroy() for t in self.legal_targets]
	self.controller.scry(2)


##############################
Auriok Sunchaser

	StaticEffects:
		'self'
			"modifyPT"
			(2, 2)
			lambda eff: len([_a for _a in eff.apply_target.controller.battlefield if _a.is_artifact]) >= 3
		'self'
			"gainAbility"
			'Flying'
			lambda eff: len([_a for _a in eff.apply_target.controller.battlefield if _a.is_artifact]) >= 3


##############################
Balamb T-Rexaur

	Triggers:
		onEtB
			self.controller.gain_life(3)


##############################
Barging Sergeant

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Barrier of Bones

	Triggers:
		onEtB
			self.controller.surveil(1)


##############################
Batterhorn

	Triggers:
		onEtB
			[t.destroy() for t in self.legal_targets]
				Targets:
					'target artifact'


##############################
Battlewise Valor

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (2, 2), self, self.game.eot_time) for t in self.legal_targets]
	self.controller.scry(1)


##############################
Bellows Lizard

	Abilities:
		1R: self.card.add_effect('modifyPT', (1, 0), self, self.game.eot_time)


##############################
Blighted Woodland

	Abilities:
		T: self.controller.mana.add(mana.Mana.COLORLESS, 1)


##############################
Blustersquall

	Targets:
		'target opponent creature'

	[t.tap() for t in self.legal_targets]


##############################
Boros Challenger

	Abilities:
		2RW: self.card.add_effect('modifyPT', (1, 1), self, self.game.eot_time)

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Boros Elite

	Triggers:
		onAttack
			self.card.add_effect('modifyPT', (2, 2), self, self.game.eot_time)
				Conditioned On:
					len([_c for _c in self.card.controller.battlefield if _c.is_creature and _c.status.is_attacking]) >= 3


##############################
Boros Mastiff

	Triggers:
		onAttack
			self.card.add_effect('gainAbility', 'Lifelink', self, self.game.eot_time)
				Conditioned On:
					len([_c for _c in self.card.controller.battlefield if _c.is_creature and _c.status.is_attacking]) >= 3


##############################
Brindle Boar

	Abilities:
		Sacrifice ~: self.controller.gain_life(4)


##############################
Burglar Rat

	Triggers:
		onEtB
			self.controller.opponent.discard(1, rand=True)


##############################
Burning-Tree Emissary

	Triggers:
		onEtB
			self.controller.mana.add(mana.Mana.RED, 1), self.controller.mana.add(mana.Mana.GREEN, 1)


##############################
Call of the Conclave

	self.controller.create_token('3/3 green Centaur', 1)


##############################
Centaur's Herald

	Abilities:
		2G, Sacrifice ~: self.controller.create_token('3/3 green Centaur', 1)


##############################
Clinging Anemones

	Triggers:
		onControllerCreatureEtB
			self.card.add_counter('+1/+1')
				If:
					self.trigger_source is not self.card and (self.trigger_source.power > self.card.power or self.trigger_source.toughness > self.card.toughness)


##############################
Conclave Guildmage

	Abilities:
		5W, T: self.controller.create_token('2/2 green And', 1)


##############################
Consign

	Targets:
		'target nonland permanent'

	[t.bounce() for t in self.legal_targets]


##############################
Counterspell

	Targets:
		'spell'

	[t.counter(source=self) for t in self.legal_targets]


##############################
Craterize

	Targets:
		'target land'

	[t.destroy() for t in self.legal_targets]


##############################
Crocanura

	Triggers:
		onControllerCreatureEtB
			self.card.add_counter('+1/+1')
				If:
					self.trigger_source is not self.card and (self.trigger_source.power > self.card.power or self.trigger_source.toughness > self.card.toughness)


##############################
Crypt of the Eternals

	Abilities:
		T: self.controller.mana.add(mana.Mana.COLORLESS, 1)

	Triggers:
		onEtB
			self.controller.gain_life(1)


##############################
Dead Weight

	Aura:
		Targets:
			'creature'

		self.add_pt((-2, -2))


##############################
Deadly Visit

	Targets:
		'target creature'

	[t.destroy() for t in self.legal_targets]
	self.controller.surveil(2)


##############################
Devious Cover-Up

	Targets:
		'spell'

	[t.counter(source=self) for t in self.legal_targets]


##############################
Devout Chaplain

	Abilities:
		T: [t.exile() for t in self.legal_targets]
			Targets:
				'target artifact or enchantment'


##############################
Dimir Locket

	Abilities:
		T, Sacrifice ~: self.controller.draw(2)


##############################
Disappearing Act

	Targets:
		'spell'

	[t.counter(source=self) for t in self.legal_targets]


##############################
Divine Favor

	Aura:
		Targets:
			'creature'

		self.add_pt((1, 3))


##############################
Divine Verdict

	Targets:
		'target attacking or blocking creature'

	[t.destroy() for t in self.legal_targets]


##############################
Dragon Hatchling

	Abilities:
		R: self.card.add_effect('modifyPT', (1, 0), self, self.game.eot_time)


##############################
Electrickery

	Targets:
		'target opponent creature'

	[t.take_damage(self, 1) for t in self.legal_targets]


##############################
Era of Innovation

	Abilities:
		Sacrifice ~: self.controller.draw(3)


##############################
Escarpment Fortress

	StaticEffects:
		'controller -self'
			"modifyPT"
			(1, 0)
			lambda eff: eff.apply_target.is_creature


##############################
Experiment One

	Triggers:
		onControllerCreatureEtB
			self.card.add_counter('+1/+1')
				If:
					self.trigger_source is not self.card and (self.trigger_source.power > self.card.power or self.trigger_source.toughness > self.card.toughness)


##############################
Experimental Aviator

	Triggers:
		onEtB
			self.controller.create_token('1/1 colorless Thopter', 2)


##############################
Fade into Antiquity

	Targets:
		'target artifact or enchantment'

	[t.exile() for t in self.legal_targets]


##############################
Fall of the Gavel

	Targets:
		'spell'

	[t.counter(source=self) for t in self.legal_targets]
	self.controller.gain_life(5)


##############################
Festerhide Boar

	Triggers:
		onEtB
			self.card.add_counter('+1/+1', 2)
				If:
					bool(self.card.controller.turn_events['creature_died'])


##############################
Flinthoof Boar

	Abilities:
		R: self.card.add_effect('gainAbility', 'Haste', self, self.game.eot_time)


##############################
Generous Stray

	Triggers:
		onEtB
			self.controller.draw(1)


##############################
Ghalma's Warden

	StaticEffects:
		'self'
			"modifyPT"
			(2, 2)
			lambda eff: len([_a for _a in eff.apply_target.controller.battlefield if _a.is_artifact]) >= 3


##############################
Ghor-Clan Savage

	Triggers:
		onEtB
			self.card.add_counter('+1/+1', 3)
				If:
					bool(self.card.controller.opponent.turn_events['damaged'])


##############################
Gideon's Lawkeeper

	Abilities:
		W, T: [t.tap() for t in self.legal_targets]
			Targets:
				'target creature'


##############################
Glowspore Shaman

	Triggers:
		onEtB
			self.controller.mill(3)


##############################
Goblin Banneret

	Abilities:
		1R: self.card.add_effect('modifyPT', (2, 0), self, self.game.eot_time)

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Golgari Raiders

	Triggers:
		onEtB
			self.card.add_counter('+1/+1', len([_c for _c in self.card.controller.graveyard if _c.is_creature]))


##############################
Gore-House Chainwalker

	Triggers:
		onEtB
			self.card.add_counter('+1/+1') if self.card.controller.may('Have ~ enter with a +1/+1 counter (Unleash)?') else None

	StaticEffects:
		'self'
			"cantBlock"
			True
			lambda eff: eff.apply_target.num_counters('+1/+1') > 0


##############################
Gorehorn Minotaurs

	Triggers:
		onEtB
			self.card.add_counter('+1/+1', 2)
				If:
					bool(self.card.controller.opponent.turn_events['damaged'])


##############################
Grappling Sundew

	Abilities:
		4G: self.card.add_effect('gainAbility', 'Indestructible', self, self.game.eot_time)


##############################
Grasping Dunes

	Abilities:
		T: self.controller.mana.add(mana.Mana.COLORLESS, 1)


##############################
Harsh Scrutiny

	self.controller.scry(1)


##############################
Hellhole Flailer

	Triggers:
		onEtB
			self.card.add_counter('+1/+1') if self.card.controller.may('Have ~ enter with a +1/+1 counter (Unleash)?') else None

	StaticEffects:
		'self'
			"cantBlock"
			True
			lambda eff: eff.apply_target.num_counters('+1/+1') > 0


##############################
Hidden Herbalists

	Triggers:
		onEtB
			self.card.controller.mana.add(mana.Mana.GREEN, 2)
				If:
					bool(self.card.controller.turn_events['permanent_left'])


##############################
Holy Mantle

	Aura:
		Targets:
			'creature'

		self.add_pt((2, 2))


##############################
House Guildmage

	Abilities:
		2B, T: self.controller.surveil(2)


##############################
Hypothesizzle

	self.controller.draw(2)


##############################
Illusionist's Stratagem

	self.controller.draw(1)


##############################
Inescapable Blaze

	Targets:
		'any target'

	[t.take_damage(self, 6) for t in self.legal_targets]


##############################
Integrity

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (2, 2), self, self.game.eot_time) for t in self.legal_targets]


##############################
Iona's Judgment

	Targets:
		'target creature or enchantment'

	[t.exile() for t in self.legal_targets]


##############################
Ironshell Beetle

	Triggers:
		onEtB
			[t.add_counter('+1/+1', 1) for t in self.legal_targets]
				Targets:
					'target creature'


##############################
Knight Watch

	self.controller.create_token('2/2 white Knight', 2)


##############################
Kor Hookmaster

	Triggers:
		onEtB
			[t.tap() for t in self.legal_targets]
				Targets:
					'target opponent creature'


##############################
Kraul Warrior

	Abilities:
		5G: self.card.add_effect('modifyPT', (3, 3), self, self.game.eot_time)


##############################
Lash of the Whip

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (-4, -4), self, self.game.eot_time) for t in self.legal_targets]


##############################
League Guildmage

	Abilities:
		3U, T: self.controller.draw(1)


##############################
Ledev Champion

	Abilities:
		3GW: self.controller.create_token('1/1 white Soldier', 1)


##############################
Legion Guildmage

	Abilities:
		5R, T: self.controller.opponent.take_damage(self, 3)


##############################
Liturgy of Blood

	Targets:
		'target creature'

	[t.destroy() for t in self.legal_targets]
	self.controller.mana.add(mana.Mana.BLACK, 3)


##############################
Lobber Crew

	Abilities:
		T: self.controller.opponent.take_damage(self, 1)


##############################
Loxodon Restorer

	Triggers:
		onEtB
			self.controller.gain_life(4)


##############################
Maniacal Rage

	Aura:
		Targets:
			'creature'

		self.add_pt((2, 2))


##############################
Maverick Thopterist

	Triggers:
		onEtB
			self.controller.create_token('1/1 colorless Thopter', 2)


##############################
Mist Raven

	Triggers:
		onEtB
			[t.bounce() for t in self.legal_targets]
				Targets:
					'target creature'


##############################
Muse Drake

	Triggers:
		onEtB
			self.controller.draw(1)


##############################
Nature's Way

	Targets:
		'target your creature'

	[(t.add_effect('gainAbility', 'Vigilance', self, self.game.eot_time), t.add_effect('gainAbility', 'Trample', self, self.game.eot_time)) for t in self.legal_targets]


##############################
New Prahv Guildmage

	Abilities:
		WU: [t.add_effect('gainAbility', 'Flying', self, self.game.eot_time) for t in self.legal_targets]
			Targets:
				'target creature'


##############################
Omenspeaker

	Triggers:
		onEtB
			self.controller.scry(2)


##############################
Parhelion Patrol

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Pilfered Plans

	self.controller.draw(2)


##############################
Portent of Betrayal

	self.controller.scry(1)


##############################
Prey Upon

	Targets:
		'target your creature'
		'target opponent creature'

	(targets[0].deals_damage(targets[1], targets[0].power), targets[1].deals_damage(targets[0], targets[1].power)) if len(targets) == 2 and all(is_legal_target) else None


##############################
Price of Fame

	Targets:
		'target creature'

	[t.destroy() for t in self.legal_targets]
	self.controller.surveil(2)


##############################
Psychic Strike

	Targets:
		'spell'

	[t.counter(source=self) for t in self.legal_targets]


##############################
Pursuit of Flight

	Aura:
		Targets:
			'creature'

		self.add_pt((2, 2))


##############################
Radical Idea

	self.controller.draw(1)


##############################
Rakdos Cackler

	Triggers:
		onEtB
			self.card.add_counter('+1/+1') if self.card.controller.may('Have ~ enter with a +1/+1 counter (Unleash)?') else None

	StaticEffects:
		'self'
			"cantBlock"
			True
			lambda eff: eff.apply_target.num_counters('+1/+1') > 0


##############################
Ray of Dissolution

	Targets:
		'target enchantment'

	[t.destroy() for t in self.legal_targets]
	self.controller.gain_life(3)


##############################
Repel the Darkness

	Targets:
		'up to two target creature'

	[t.tap() for t in self.legal_targets]
	self.controller.draw(1)


##############################
Righteous Blow

	Targets:
		'target attacking or blocking creature'

	[t.take_damage(self, 2) for t in self.legal_targets]


##############################
Rites of Reaping

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (3, 3), self, self.game.eot_time) for t in self.legal_targets]


##############################
Rix Maadi Guildmage

	Abilities:
		BR: [t.add_effect('modifyPT', (-1, -1), self, self.game.eot_time) for t in self.legal_targets]
			Targets:
				'target blocking creature'


##############################
Satyr Hedonist

	Abilities:
		R, Sacrifice ~: self.controller.mana.add(mana.Mana.RED, 3)


##############################
Savage Surge

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (2, 2), self, self.game.eot_time) for t in self.legal_targets]


##############################
Searing Spear

	Targets:
		'any target'

	[t.take_damage(self, 3) for t in self.legal_targets]


##############################
Selesnya Locket

	Abilities:
		T, Sacrifice ~: self.controller.draw(2)


##############################
Sensor Splicer

	Triggers:
		onEtB
			self.controller.create_token('3/3 colorless Phyrexian', 1)


##############################
Shielded Aether Thief

	Abilities:
		T: self.controller.draw(1)


##############################
Siege Modification

	Aura:
		Targets:
			'creature'

		self.add_pt((3, 0))


##############################
Solemn Offering

	Targets:
		'target artifact or enchantment'

	[t.destroy() for t in self.legal_targets]
	self.controller.gain_life(4)


##############################
Splatter Thug

	Triggers:
		onEtB
			self.card.add_counter('+1/+1') if self.card.controller.may('Have ~ enter with a +1/+1 counter (Unleash)?') else None

	StaticEffects:
		'self'
			"cantBlock"
			True
			lambda eff: eff.apply_target.num_counters('+1/+1') > 0


##############################
Stand Firm

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (1, 1), self, self.game.eot_time) for t in self.legal_targets]
	self.controller.scry(2)


##############################
Stir the Sands

	self.controller.create_token('2/2 black Zombie', 3)


##############################
Sunhome Stalwart

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Survey the Wreckage

	Targets:
		'target land'

	[t.destroy() for t in self.legal_targets]
	self.controller.create_token('1/1 red Goblin', 1)


##############################
Swarm Guildmage

	Abilities:
		1G, T: self.controller.gain_life(2)


##############################
Sword Dancer

	Abilities:
		WW: [t.add_effect('modifyPT', (-1, 0), self, self.game.eot_time) for t in self.legal_targets]
			Targets:
				'target attacking creature'


##############################
Take Heart

	Targets:
		'target creature'

	[t.add_effect('modifyPT', (2, 2), self, self.game.eot_time) for t in self.legal_targets]


##############################
Talrand's Invocation

	self.controller.create_token('2/2 blue Drake', 2)


##############################
Tenth District Guard

	Triggers:
		onEtB
			[t.add_effect('modifyPT', (0, 1), self, self.game.eot_time) for t in self.legal_targets]
				Targets:
					'target creature'


##############################
The Crystal's Chosen

	self.controller.create_token('1/1 colorless Hero', 4)


##############################
Timberland Guide

	Triggers:
		onEtB
			[t.add_counter('+1/+1', 1) for t in self.legal_targets]
				Targets:
					'target creature'


##############################
Tireless Missionaries

	Triggers:
		onEtB
			self.controller.gain_life(3)


##############################
Truefire Captain

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Turn to Slag

	Targets:
		'target creature'

	[t.take_damage(self, 5) for t in self.legal_targets]


##############################
Undercity Uprising

	Targets:
		'target your creature'
		'target opponent creature'

	(targets[0].deals_damage(targets[1], targets[0].power), targets[1].deals_damage(targets[0], targets[1].power)) if len(targets) == 2 and all(is_legal_target) else None


##############################
Unexplained Disappearance

	Targets:
		'target creature'

	[t.bounce() for t in self.legal_targets]
	self.controller.surveil(1)


##############################
Unsummon

	Targets:
		'target creature'

	[t.bounce() for t in self.legal_targets]


##############################
Urban Evolution

	self.controller.draw(3)


##############################
Urbis Protector

	Triggers:
		onEtB
			self.controller.create_token('4/4 white Angel', 1)


##############################
Veiled Shade

	Abilities:
		1B: self.card.add_effect('modifyPT', (1, 1), self, self.game.eot_time)


##############################
Vengeful Rebel

	Triggers:
		onEtB
			[t.add_effect('modifyPT', (-3, -3), self, self.game.eot_time) for t in self.legal_targets]
				If:
					bool(self.card.controller.turn_events['permanent_left'])
				Targets:
					'target opponent creature'


##############################
Vessel of Endless Rest

	Abilities:
		T: self.controller.mana.add(mana.Mana.COLORLESS, 1)


##############################
Volt Charge

	Targets:
		'any target'

	[t.take_damage(self, 3) for t in self.legal_targets]


##############################
Wall of Fire

	Abilities:
		R: self.card.add_effect('modifyPT', (1, 0), self, self.game.eot_time)


##############################
Wand of Vertebrae

	Abilities:
		T: self.controller.mill(1)


##############################
Whisper Agent

	Triggers:
		onEtB
			self.controller.surveil(1)


##############################
Wild Guess

	self.controller.draw(2)


##############################
Wojek Bodyguard

	Triggers:
		onAttack
			[t.add_counter('+1/+1') for t in self.legal_targets]
				Targets:
					p.is_creature and p.status.is_attacking and p is not self.card and p.power < self.card.power


##############################
Zhur-Taa Druid

	Abilities:
		T: self.controller.mana.add(mana.Mana.GREEN, 1)


##############################
